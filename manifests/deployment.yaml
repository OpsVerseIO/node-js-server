apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-js-server
  labels:
    app: node-js-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: node-js-server
  template:
    metadata:
      labels:
        app: node-js-server
    spec:
      containers:
      - name: node-js-server
        image: docker pull registry.devopsnow.io/internal/node-js-server:d676c0b2
        ports:
        - containerPort: 3000